
    . cleanup old auth tokens

    . cache for link details, memcached
    . don't add duplicate links, but refresh old one
    . async link details fetching

    . don't retrieve not needed url info, only description, title and MAYBE favicon